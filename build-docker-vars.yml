variables:
  BUILD_IMAGE: docker:20.10
  DOCKER_BUILDKIT: 1
  DOCKER_FILE_PATH: docker/Dockerfile
  DOCKER_BUILD_CONTEXT_PATH: .
  # Like a: --build-arg SOME_OPTS=some_value
  DOCKER_BUILD_ADDITIONAL_OPTS: ""
  DOCKER_BUILD_OMIT_AMD64: "no"
  DOCKER_BUILD_OMIT_ARM64V8: "no"
  DOCKER_BUILD_OMIT_MANIFEST: "no"
